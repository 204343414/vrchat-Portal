# 新 Chat 简报：粒子相对传送独立脚本

> 用途：这份文档是给你（新 chat 的 LLM）的开场资料。任务是在一个**全新的、独立的 .cs** 上
> 只做"粒子相对传送"，成功后合并回 `双向传送门管理器.cs`。
> 仓库：https://github.com/204343414/vrchat-Portal（分支 arena/01a011f0-vrchat-portal，当前 head 0ccf6e6）。
> 参考实现就在仓库的 `双向传送门管理器.cs` 里，粒子部分关键词：
> `ProcessParticleTeleports` / `ProcessSingleParticleSystem` / `ParticleSegmentCrossesPortal` /
> `TryBounceCapturePortal` / `TeleportParticleThroughPortal` / `GetOrAssignPairingSlot`。
> 交接文档（历史决策记录）在 `Editor/传送门中文面板.cs` 顶部注释里，十六轮~二十五论是粒子部分。

---

## 一、目标（就这一件事）

独立 UdonSharp 脚本（单文件、`UdonSharpBehaviour`、不依赖现有管理器），实现**粒子相对传送**：
粒子穿过"门 A 平面"→ 以 **from→to + 经典半转**（`to * halfTurn * from^-1`，halfTurn=绕Y轴180°）的
相对映射传送到 B 门侧，位置和速度都用同一映射——**入射角=反射角（相对门坐标系）**。

验收标准（用户实测验证过的方法）：
- 直线光束粒子，速度 10 / 100 / 1000 都 **零漏捕**；
- 粒子系统 Simulation Space = **Local 和 World 都正确**（自动识别，不许手拖清单）；
- 出口是**一根连续直线**：方向正确、无散射、无断层、贴门面（0.06m）；
- 开碰撞的粒子被墙弹回也能捕获（借 Unity 碰撞反弹做检测信号）；
- 零配置：拖两个门 Transform + 开一个调试开关就能测。

---

## 二、已证实的正确架构（24 轮迭代产物，可直接照抄算法）

### 2.1 数据通道
`ps.GetParticles(buffer)` / `ps.SetParticles(buffer, count)` —— Udon 里**唯一**能读写活粒子的 API。
粒子模拟本体在 C++ 引擎层每帧推进一次，脚本只能读帧末快照。可用字段（纯数据结构体，实测可读）：
`position`、`velocity`、`randomSeed`（出生分配、终生不变的粒子身份证）、`startSize`、
`startLifetime`、`remainingLifetime`。

### 2.2 身份配对（跨帧识别"同一颗粒子"）
- 按槽位存 `prevPositions / prevSeeds / prevValid / prevVelocities`，用 `randomSeed` 配对。
- **必须按系统隔离**：16 系统 × 1024 粒子跨度的平铺数组（`slot*1024 + i`）。所有系统共用
  一套数组 = 帧内互相覆盖 = 配对全体失效（规则4永不触发、规则1退化）——这是踩过的大坑。
- 配对失败（首见/槽位洗牌/池满）→ 用速度反推线段起点（新生粒子反推出生点，老粒子反推一帧）。

### 2.3 四条捕获规则（覆盖"粒子进入门洞"的全部方式）
- **规则1 本帧穿越**：位移线段两端跨检测面符号翻转 → 插值求穿越点，穿越点在门框内 → 传送。
  对直线运动数学精确、与速度无关（线段就是两帧间的真实路径）。
- **规则2 门洞捕获**：`门框内 && 门平面后侧(z<0)` → 传送，**深度不限**。
  覆盖"框外穿面、之后横向飘进框"（漂移入框）——深度窗是漏捕根源，必须无限深。
- **规则3 首见后侧兜底**：未配对粒子 + 框内 + 门后 → 传送。
- **规则4 反弹捕获 v2**：配对粒子门法向速度一帧内反转（朝门→离门，阈值±0.1）时，
  反解反弹时刻与反弹点：`tb = (z1 - z0 - vn1*dt) / (vn0 - vn1)`，`bouncePoint = prevPos + prevVel*tb`，
  验证反弹点在门框内且 z∈[-0.4, 1.2] 容差带 → 传送，映射速度用**反弹前**速度。
  数值模拟验证过：重构误差≈机器精度 1e-14、零假阳性。这是"开碰撞粒子先弹走碰不到传送"
  的唯一解法（旧"帧末位置窗口"法对高速反弹漏捕 90.2%）。

### 2.4 检测面与门框
- 检测面 = 门平面沿 forward 外推 `particleTeleportPlaneOffset`（默认0.05）。
- 门框判定按**每颗粒子 startSize/2 外扩**（上限1m）——粒子是面片不是点，
  中心擦框外、身体压框内的粒子会被漏。规则1/2/3/4 统一用外扩判定。

### 2.5 出射锚定（决定"出口直线不散不断"的四条铁律）
1. 捕获点 = **沿粒子自身运动线反推与检测面的交点**（`hit = segEnd - vel*(zEnd/vn)`），
   直线运动下精确=真实过门点。同一条光束的所有粒子（无论哪条规则抓到）映射到同一个点。
2. 锚点统一**回移 offset 到真实门平面 z=0** 再映射。
3. 出口前推**沿出口光束方向**，法向分量恰好 = `offset + 0.01`（≈0.06m）。
   这是防回穿的数学下限：必须越过出口检测面，否则下帧线段会再穿检测面被规则1回抓=乒乓。
   近垂直入射时贴门面0.06m，可见死区最小；切线入射推距封顶2m后补沿出口法线推足。
4. **禁止**"穿越后剩余时间积分"（`remainingTime`）——它把出射粒子沿光束推出 `速度×dt` 米
   （1000m/s 低帧率达几十米），门面留无粒子缝隙=断层，速度越快越明显。
   代价：粒子传送帧损失 (1-t)×dt 的运动，次帧从出口继续，不可见。

### 2.6 Local/World 自动识别
`int space = (int)ps.main.simulationSpace;` —— **枚举值（以 Unity 参考源码
ParticleSystemEnums.cs 为准）：Local=0、World=1、Custom=2**（注意别写反，写反过一次：
World 粒子被双重变换、Local 粒子被按世界坐标直读 → "传送 0 次"回归）。
- World=1：直通；Local=0：粒子坐标 `sysT.TransformPoint/Vector` 转世界判定，写回前
  `InverseTransformPoint/Vector` 转回局部（配对记录永远存世界坐标）；
- Custom=2：按 Local 处理（假设 customSimulationSpace=自身 Transform，警告一次）。

### 2.7 禁止时间型免疫帧
Portal 语义要求激光可反复套娃穿门（A→B→A…），帧数免疫会吞掉第三次合法进洞=制造新穿墙。
防回弹只靠状态型守卫：出口前推越检测面 + 半转后出口速度必然背离出口门平面 + 规则本身状态型。

---

## 三、Udon API 白名单事实（查证过，别在同一个坑里再摔）

| API | 状态 | 证据 |
|---|---|---|
| `ps.GetParticles` / `ps.SetParticles` | ✅ 可用 | 主代码生产使用中 |
| `ps.main.simulationSpace` | ✅ 可用 | 编译探针实测通过（曾误判为白名单风险，是假的） |
| `Particle` 结构体字段（randomSeed/startSize/position/velocity/lifetime） | ✅ 可用 | 生产使用中 |
| `GameObject.Find(名字)` | ✅ 可用 | 编译探针实测通过（只能找**激活**物体） |
| `OnParticleTrigger` | ❌ 编译不过 | CS0115 实测记录在案 |
| `OnTriggerEnter`（物理触发器） | ❌ 对粒子不触发 | 粒子不是刚体，不产生物理触发事件 |
| `ParticlePhysicsExtensions`（碰撞事件/碰撞点） | ❌ 未开放 | VRChat 反馈板至今 tracked：https://feedback.vrchat.com/udon/p/expose-particlephysicsextensions-that-helps-particle-collision-detection |
| 碰撞模块参数（`ps.collision.radiusScale` 等） | ❌ 不可读 | 模块结构体不暴露——碰撞半径只能用 `startSize` 近似 |
| `FindObjectsOfType` / `Scene.GetRootGameObjects` | ❌ 禁用 | 编译报错实测 |
| List/Dictionary/Linq | ❌ 不可用 | 用固定数组（Udon 惯例） |
| `Vector3.ToString("F2")` 格式重载 | ⚠️ 未实证 | 用 `float.ToString("F2")` 拼串 |

---

## 四、十轮迭代踩过的坑（每条都是实测+日志定位，别重蹈）

1. **枚举值**：`ParticleSystemSimulationSpace` Local=0/World=1/Custom=2（写反过一次，直接"传送0次"）。
2. **配对数组必须按系统隔离**（见 2.2），共享数组=静默失效。
3. **深度窗（retroWindow）是漏捕根源**：漂移入框在高速时发生在窗外深处，任何有限窗都追不上
   （速度1000实测漂到49米深才进框）。正确语义：框内+门后=捕获，深度不限。
4. **捕获点不能用"粒子当前深位置"映射**（深度沿出口法线带出→出口甩出30~60米="两根筷子"），
   也不能用"沿法线投影"（保留当前随机 x/y→出口在框内随机散射="水中折射"），
   必须**沿运动线反推过面点**（2.5 第1条）。
5. **出口前推的量与方向**：沿法线推→斜射整条光束横移（断层）；沿光束推但量0.2m→近垂直入射
   留0.2m可见死区。终版：沿光束、法向分量=offset+0.01。
6. **remainingTime 积分**→门面缝隙随速度变大（断层主犯），必须删。
7. **帧内去重**：同一系统可能同时出现在两个注册列表，同帧处理两次→检测计数翻倍+配对记录
   重复写回。需要每帧重置的已处理列表。
8. **场景序列化的字段值不会跟随代码默认值变化**：改默认值后，老场景 Inspector 里的值还是旧的。
9. **传送后免疫帧被用户否决**（套娃语义，见 2.7）。
10. **排查要信日志不信肉眼**：1粒/秒 + 寿命2秒（空中不攒旧瞄准的"幽灵粒子"）+ 测试中别挪门。
    日志字段按下面 5.2 的格式打，每颗粒子的事件/嫌疑行都带种子号，能直接对账。

---

## 五、验收测试与日志规范（沿用被证明有效的）

### 5.1 测试矩阵
- 发射率：1粒/秒（对账用）和 187粒/秒（压力用）
- 速度：10 / 100 / 1000（1000 是目标）
- 入射角：90°（垂直）+ 81.7° / 93.4°（用户实测过散射的角度）
- 碰撞：关 和 开（开了走规则4）
- 空间：World 系统 和 Local 系统
- 粒子寿命调短到2秒；测试中途不要移动两扇门

### 5.2 日志格式（新脚本照抄这套，排查效率已被验证）
- 事件行：`[粒子传送][事件] 规则N 门→门 系统=名 种子=号 入深=x 出射面距=y 出射=(x,y,z)`
- 嫌疑行：`[粒子传送][漏检嫌疑] 系统=名 zA= zB= 框内A= 框内B= 框边距A= 框边距B= 配对= 已传过= 速度= 种子=`
- 汇总行（每60帧）：`检测 N 粒子次，传送 N 次（其中反弹捕获 N），门后框内滞留 N 颗次，缓冲扩容 N 次`
- 事件日志限流：每60帧窗口打印前25条+之后每100条1条，防止刷屏

### 5.3 判定标准（数据结论，不靠肉眼）
- **滞留=0**：门后框内的未传送粒子为零 = 零漏捕（框外后侧的粒子是射偏，不算漏；
  "已传过=True"的嫌疑行是从出口飞过墙区的粒子，噪音）；
- **出射面距≈±(offset+0.01)≈0.06**：出射锚定正确（远大于此=锚定异常）；
- **同一光束所有粒子出射点收敛到同一过门点**：出口连续直线。

---

## 六、诚实边界（引擎层面修不掉，别浪费时间）

1. **"穿一小段短距离才被传"**：帧采样固有边界——粒子穿面后首个采样帧的深度≈速度×dt
   （100m/s≈1.7m，1000m/s≈16.7m），渲染一帧后即被传走。不是漏（滞留=0），
   除非引擎开放粒子回调，无解。
2. **触发器级兜底不存在**：OnParticleTrigger 编译不过、OnTriggerEnter 对粒子不触发、
   ParticlePhysicsExtensions 未开放（见第三节表）。
3. **亚帧生灭**（寿命<1帧的粒子）引擎层面不可观测。
4. **配对池上限**：16系统×1024粒子，超出部分按未配对处理（反推线段仍精确，仅反弹捕获降级）。
5. **Custom 空间按"自定义空间=自身Transform"假设**。
6. **倒退穿门闪屏**是渲染课题，**与本脚本无关，不要碰**（主项目遗留，后续单独处理）。

---

## 七、合并回主代码时的接口约定

独立脚本建议暴露：`portalPlaneA/portalPlaneB`（Transform）、`portalWidth/portalHeight`、
`particleTeleportPlaneOffset`、`useClassicHalfTurn`、`debugLog`，以及一个注册粒子系统列表的
入口（主代码现在的注册来源：root扫描 / 放置发现OverlapSphere / 收集根 / 黑名单排除根）。
合并时主代码把注册结果喂给独立脚本即可；`particleTeleportRetroWindow` 语义保留"<=0=深度不限"
（无限深度是正确解），老场景序列化值 0.5 记得迁到 0。

**注意与主代码无关的字段**：`teleportTriggerOffset` 是【玩家】传送触发偏移（用户当前设0.1，
前进视角实测最佳），粒子检测面偏移是 `particleTeleportPlaneOffset`，两个别混。

---

## 八、一句话总结给新 chat

> 在 Udon 里做"完美的粒子相对传送"= 采样扫描 + randomSeed 按系统隔离配对 + 四条捕获规则
> （线段穿越/框内门后无限深/未配对兜底/反弹点重建）+ 出射锚定沿运动线反推过面点 +
> 出口沿光束前推(offset+0.01)。零漏的数学保证来自"线段就是两帧间的真实路径"和
> "框内+门后=进了门洞"这两个不变量；剩下全是上面列的十个坑。
